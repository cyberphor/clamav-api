# enable logging
LogFile /var/log/clamav.log
LogTime yes

# set path to socket file where daemon will listen
LocalSocket /tmp/clamd.sock

# set socket permissions so it is not world-readable
LocalSocketMode 660